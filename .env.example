# External provider keys (required)
ORS_API_KEY=
OCM_API_KEY=
TOMTOM_API_KEY=
YELP_API_KEY=

# Optional: HTTP timeout in seconds for external provider calls
EXTERNAL_API_TIMEOUT_SECONDS=10

# Existing backend auth config (already used in app)
FIREBASE_PROJECT_ID=
FIREBASE_SERVICE_ACCOUNT_JSON=
GOOGLE_APPLICATION_CREDENTIALS=
